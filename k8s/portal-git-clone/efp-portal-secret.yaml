apiVersion: v1
kind: Secret
metadata:
  name: efp-portal-secret
  namespace: default
stringData:
  BOOTSTRAP_ADMIN_PASSWORD: ""
  GIT_TOKEN: ""
  # Generate once and keep stable across SECRET_KEY rotations.
  RUNTIME_INTERNAL_SECRET: ""
